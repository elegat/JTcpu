汇编器使用方法

本汇编器被设计为一个命令行程序.

使用方法是先编辑你的汇编源程序，以jt为扩展名进行保存，比如myasm.Jt，然后（以Windows为例），
打开命令行窗口，输入 jtas myasm 或者 jtas myasm.Jt，回车就可以进行汇编，
如果有错误将会被打印出来，如果没有错误，那么将会在源程序的目录下生成myasm.Hex文件，如果该文件已存在，将会被覆盖。
如果需要生成其他名字的hex文件，那么使用参数 -o name 将会生成name.Hex文件，
如果需要生成verilog格式的Rom初始化文件，只需要添加参数 -v 即可，做一个演示： Jtas myasm -o name -v
